services:
  backend:
    environment:
      TEKDOCS_BOOTSTRAP_TOKEN: ""
      TEKDOCS_BOOTSTRAP_TOKEN_FILE: /run/secrets/bootstrap_token
    secrets:
      - bootstrap_token

secrets:
  bootstrap_token:
    file: ${TEKDOCS_SECRET_DIRECTORY:?Set TEKDOCS_SECRET_DIRECTORY}/bootstrap_token
