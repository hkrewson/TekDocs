# TekDocs 0.4.6 circuit and handoff evidence

Date: 2026-08-11

Status: **implementation and applicable local evidence complete.** No push, tag, image publication, deployment, or other external change was performed. This chronological build delivers the deferred `0.4.4` capability without downgrading the prior `0.4.5` application version.

## Delivered scope

- Stable exact-Workspace circuit and A/Z handoff Entities with provider, optional same-provider contract, service identity, bandwidth, lifecycle, and physical/logical placement.
- Deterministic review, disconnect, renewal-notice, renewal, and contract-end projections; contract-derived data is permission-controlled and all costs remain absent.
- Non-aggregating MSP/client APIs and a restrained searchable Circuits surface with create/edit, lifecycle, contract, and handoff workflows.
- Central network permissions, MFA-backed mutation, forced PostgreSQL RLS, relationship guards, advisory locking, bounded collections, value-minimized audit data, and IDOR/direct-write coverage.

## Known boundaries

- Reminder dates are projections only. Delivery through in-app or email notifications remains assigned to the later notification outbox milestones.
- TekDocs does not store circuit credentials, validate provider service state, query carrier APIs, process invoices, or expose contract costs through Network routes.
- Circuit recycle-bin support, diagrams, NetBox reconciliation, bulk transfer, scale/restore work, and final network certification remain assigned to `0.4.7`–`0.5.0`.
- `TD-RISK-035` is initially mitigated; later network stabilization must retain its negative and scale evidence.

## Verification

- `make check` passed the version, supply-chain, action lint, Ruff, mypy, migration, OpenAPI, frontend lint/type/test/build, accessibility, and bundle gates.
- `make test` passed 1,255 backend tests with 37 intentional skips and 80.22% coverage, plus 190 frontend tests across 46 files with the configured coverage thresholds.
- `make test-networks` passed the complete PostgreSQL network, permission/IDOR, forced-RLS, migration, direct-write, and concurrency suite.
- The isolated Chromium real-stack journey created and reloaded a linked circuit and exact interface handoff through React and Django; a separate database assertion verified Workspace ownership, provider/contract provenance, lifecycle date, and site/device/interface edges in PostgreSQL.
