# TekDocs 0.3.0 reusable-documentation certification evidence

Date: 2026-08-09

Status: **implementation complete; certification evidence is recorded below.** No push, tag, image publication, deployment, Wiki publication, or other external change was performed.

## Certified scope

- Canonical technical Markdown with accessible visual/raw/preview/help modes and strict server/client sanitization.
- Stable reusable blocks, immutable checksummed revisions, optimistic concurrency, paginated history, live/pinned composition, bounded transclusion, backlinks, reuse impact, permission-aware shared editing, detach, and stable entity mentions.
- Workspace-owned title-first documentation, explicit MSP-to-client listing references, categories, reusable templates, managed private attachments, and deterministic Markdown import/export.
- Immutable STATIC publications with exact dependency resolution, canonical manifests and snapshots, Ed25519 signatures, retained deterministic PDFs and attachment bytes, required audience/reason/retention metadata, and append-only correction chains.
- One-MSP-per-installation authorization, client isolation, forced PostgreSQL RLS, migration preservation, local production-shaped runtime, and alpha-scale history performance.

## Certification mechanism

- Added `make test-documentation-certification` as one PostgreSQL composition of document/publication, rendering-abuse, authenticated-route/IDOR, runtime-RLS, and reference-performance suites.
- Added that gate to the local complete release gate and the hosted Compose workflow specification.
- Preserved legacy `0.1.3` publication-key bytes while normalizing their standard-Base64 representation to the URL-safe form required by current production validation; failed upgrade rehearsals now print bounded service diagnostics before destroying their isolated stack.
- Added no model, migration, route, dependency, Markdown extension, editor behavior, or publication format.

## Risk disposition

- `TD-RISK-003` is **satisfied for the current critical real-stack journey**, but each later critical workflow still needs its own non-mocked coverage.
- `TD-RISK-006` is **open**: the database/media rehearsal proves the current documentation recovery scope, not supported encrypted backup, destructive safeguards, scheduling, remote retention, or key-loss recovery.
- `TD-RISK-010` is **externally blocked**: hosted workflow and branch-protection evidence cannot exist until publication is authorized.
- `TD-RISK-013` is **mitigated with a remaining owner**: lazy loading and executable bundle budgets are certified; representative-device performance remains `0.8.5`.
- `TD-RISK-017` is **mitigated with a remaining owner**: 2,500-revision query/latency evidence is certified; full 250,000-revision and concurrency evidence remains `0.8.5`/`0.9.4`.
- `TD-RISK-019` is **mitigated locally with a remaining owner**: executable help drift is blocked; GitHub Wiki publication/contextual help remains `0.8.8`.
- `TD-RISK-021` is **open**: attachment validation and forced download are certified, while provider hardening and malware quarantine are now scheduled for `0.3.9`/`0.3.11`.

## Known boundaries

- “Certified” means the repository's documented local gates passed for the implemented scope. It is not third-party certification, a penetration-test result, or a legal-signature designation.
- Customer credential values remain outside TekDocs. Provider-neutral external references begin in `0.3.1`.
- Final capacity, concurrency, hosted multi-MSP isolation, encrypted backup tooling, malware quarantine, public Wiki material, published artifacts, SBOM/provenance attestations, and DAST/CodeQL hosted evidence remain later work.

## Verification

`make release-gate` passed as one uninterrupted run on 2026-08-09 after the release metadata and upgrade-compatibility fixes were applied.

- Version metadata, dependency/supply-chain pins, GitHub workflow syntax, Ruff, mypy, migration drift, and generated OpenAPI drift passed at `0.3.0`.
- All 141 frontend unit/component tests passed. The production shell was 477,087 bytes against its 512,000-byte limit; the lazy editor was 1,151,187 bytes against its 1,228,800-byte limit.
- All 669 PostgreSQL-backed backend tests passed with 90.54% coverage against the enforced 80% floor.
- `make test-documentation-certification` passed the combined document/publication, hostile-rendering, authorization/IDOR, forced-RLS, and revision-history performance matrix.
- All 96 Chromium, Firefox, and WebKit browser tests passed, followed by the non-mocked Chromium-to-Django-to-PostgreSQL workspace journey.
- `pip-audit`, Bandit, npm audit, Gitleaks, and Trivy passed with no detected dependency, static-analysis, secret, High, or Critical container finding in their configured scopes.
- The production-target image and clean-install rehearsals passed.
- The oldest-supported `0.1.3` database upgraded to `0.3.0` with identity, authentication, encrypted MFA, audit, organization, scope-guard, and constraint invariants preserved.
- The `0.2.8` documentation upgrade preserved document/block/publication identities, history, attachment and PDF bytes, and signature verification.
- The independent PostgreSQL-plus-media backup/restore rehearsal restored the database, revision history, attachment, signed manifest, and retained PDF while the required deployment keys remained separate.

The local Compose deploy check continues to emit the two documented non-fatal OpenAPI enum-name warnings and the expected HTTP-only local-stack SSL-redirect warning. Hosted CI and CodeQL, DAST, public artifacts, and deployment were not run or inferred from this local certification.
