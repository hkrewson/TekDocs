services:
  migrate:
    image: ${TEKDOCS_BACKEND_IMAGE:?Set TEKDOCS_BACKEND_IMAGE to the published GHCR digest}
    build: !reset null
  backend:
    image: ${TEKDOCS_BACKEND_IMAGE:?Set TEKDOCS_BACKEND_IMAGE to the published GHCR digest}
    build: !reset null
  worker:
    image: ${TEKDOCS_BACKEND_IMAGE:?Set TEKDOCS_BACKEND_IMAGE to the published GHCR digest}
    build: !reset null
  scheduler:
    image: ${TEKDOCS_BACKEND_IMAGE:?Set TEKDOCS_BACKEND_IMAGE to the published GHCR digest}
    build: !reset null
  frontend:
    image: ${TEKDOCS_FRONTEND_IMAGE:?Set TEKDOCS_FRONTEND_IMAGE to the published GHCR digest}
    build: !reset null
  diagram-renderer:
    image: ${TEKDOCS_RENDERER_IMAGE:?Set TEKDOCS_RENDERER_IMAGE to the published GHCR digest}
    build: !reset null
