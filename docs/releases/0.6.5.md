# TekDocs 0.6.5 sync jobs

Date: 2026-08-12

Status: **local slice complete.**

Adds durable manual/scheduled provider-page jobs, connection-scoped idempotency keys, bounded cursors, ten-minute leases, exponential retry, eight-attempt dead letters, and continuation jobs. Raw provider objects are reduced to immutable type/ID/SHA-256 observations before persistence.

Evidence is consolidated with the coordinated runtime track in `docs/releases/0.6.8.md`.
