# TekDocs 0.6.8 sanitized Git export

Date: 2026-08-12

Status: **local coordinated track complete.** No push, tag, deployment, NetBox request, exported-content Git commit, or Git remote write is implied.

## Delivered boundary

- MSP and organization Integrations surfaces now expose read-only provider connections, durable jobs, safe operational events, explicit reconciliation, sanitized Git bundles, and existing signed webhooks.
- Deterministic ZIPs use stable paths, sorted entries, fixed timestamps, canonical Markdown, STATIC manifests, and SHA-256 digests.
- Credential/1Password links, live-attachment links and content, secrets, audits, provider payloads, and editor HTML are excluded; STATIC manifests containing credential-reference metadata are rejected. Admitted signed manifests retain their evidence metadata unchanged. A bundle is suitable for an operator-controlled Git working tree but TekDocs does not create commits or hold remote credentials.
- Six new exact-Workspace tables carry PostgreSQL ownership triggers and forced RLS.

## Verification

- The composed PostgreSQL integration, comprehensive IDOR permission matrix, forced-RLS, and migration-stabilization suites pass together. They prove one-time encrypted connection setup, exact-client listings, authorization-before-validation, idempotent successful/retryable jobs, value-minimized observations/logs/conflicts, and byte-identical sanitized exports.
- Ruff, mypy across 126 Python source files, migration drift, OpenAPI drift, and the focused Bandit scan pass in pinned containers. The authoritative frontend gate passes API drift, ESLint, TypeScript, 212 unit/component tests with coverage thresholds, and its production build; Integrations is a 26.29 kB lazy-loaded chunk and both shell/editor budgets remain green.
- A fresh Compose rebuild applies migrations and starts PostgreSQL, Valkey, Django, Celery worker/scheduler, and frontend successfully; the proxied readiness endpoint reports database-ready version `0.6.8`.
- Remaining integration-runtime load, adversarial egress, exact-prior upgrade/restore, broad IDOR/RLS regression, and production-image evidence belong to `0.6.9`; composed API/integration certification remains `0.7.0`.
