# TekDocs 0.5.4 transactional event outbox

Date: 2026-08-11

Status: **applicable local slice complete.** No push, tag, published image, deployment, hosted workflow, or external security review was performed.

## Delivered boundary

- Client invitation and client-visible publication transitions atomically enqueue fixed-schema events.
- Immutable event identity/payload, stable tenant idempotency keys, forced RLS, and same-tenant organization guards protect storage.
- The scheduled worker uses bounded claims, stale-claim recovery, one append-only internal-consumer receipt, exponential retry, safe error codes, and a five-attempt dead-letter boundary.
- The outbox stores no email address, document title/body, publication reason, credential, exception message, or channel template.
- This slice creates no user-visible notification inbox and sends no new email; those remain `0.5.5` and `0.5.6`.

## Evidence

- PostgreSQL tests cover transactional rollback, idempotent replay, key reinterpretation denial, payload rejection, successful dispatch, stale recovery, retry, dead letter, append-only receipts, direct-write payload immutability, and cross-tenant RLS.
- Existing invitation, publication, portal, and permission suites cover the integrated domain transitions.
- The exact `0.5.3` production-shaped upgrade retains its historical organization, applies both outbox migrations, and delivers one newly enqueued event exactly once.
- `make test-outbox` is a named release gate alongside migration, type, lint, frontend, and version checks.
