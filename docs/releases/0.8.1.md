# TekDocs 0.8.1 encrypted recovery

Date: 2026-08-13

Status: **local slice complete.** No push, tag, deployment, remote retention configuration, or public release is implied.

## Delivered boundary

- Authenticated streaming AES-256-GCM recovery artifacts for PostgreSQL, managed media, and an allowlisted deployment-key set.
- A separately generated recovery key that must remain outside the deployment-secret directory and is rejected when link-backed or accessible to group/other users.
- An authenticated non-secret manifest, artifact checksums, atomic backup-set creation, and fail-closed producer/encryptor handling without plaintext host staging.
- A restore that authenticates all inputs before destructive work, requires exact project confirmation, refuses overwrite of recovered secrets, limits archive members, restores clean project volumes, reapplies non-owner runtime privileges, migrates, and health-checks.
- A production-target rehearsal covering representative retained state, plaintext leakage, missing confirmation, wrong-key rejection, independent volumes, recovered keys, and final application verification.

## Evidence

- `make supported-recovery-rehearsal`
- `pytest apps/core/tests/test_recovery_archive.py -q`
- focused Ruff and shell syntax checks
- `make check`

`TD-RISK-006` is mitigated for operator-invoked encrypted full recovery with a recurring owner. Scheduled/off-host retention, database/media point-in-time recovery, deliberate key-loss exercises, and recurring disaster-recovery evidence remain assigned to `0.9.3`.
