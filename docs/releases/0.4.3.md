# TekDocs 0.4.3 network endpoint evidence

Date: 2026-08-11

Status: **implementation and applicable local evidence complete.** No push, tag, image publication, deployment, or other external change was performed.

## Delivered scope

- Stable exact-Workspace interfaces, canonical IP hosts, canonical EUI-48 MAC records, and optional interface assignments.
- Routing-namespace-aware IP conflict detection, Workspace-wide MAC conflict detection, subnet containment and IPv4 reserved-host validation, and safe prefix-change behavior.
- Non-aggregating MSP/client APIs and restrained searchable Interfaces, IP addresses, and MAC addresses surfaces.
- Central network permissions, forced PostgreSQL RLS, shared advisory locks, database ownership/canonical/conflict guards, value-safe audits, and property/concurrency/IDOR coverage.

## Known boundaries

- This is operator-maintained inventory, not DHCP, ARP/NDP, switch discovery, DNS authority, or reachability monitoring. EUI-64 and vendor/OUI enrichment are not included.
- Circuits, wireless/DNS records, diagrams, NetBox reconciliation, bulk transfer, recycle-bin behavior, and scale/restore certification remain later `0.4.x` slices.
- `TD-RISK-032` remains mitigated and `TD-RISK-033` is initially mitigated; both retain `0.4.8` scale/recovery and `0.5.0` certification owners.

## Verification

- The focused network suite passed against PostgreSQL, including canonical IPv4/IPv6 and MAC properties, direct database conflict rejection, concurrent duplicate-IP serialization, sibling-client IDOR, permission inventory, forced RLS, and migration reversal/reapplication.
- All 187 frontend tests, lint, type checks, accessibility assertions, production build, and the 512 KB shell budget passed. Network-only code remains behind the lazy Networks route; the shell is 509.77 KB and the Networks chunk is 34.51 KB.
- The production-shaped Compose gate built and started every service, verified readiness and SMTP, ran Django deployment checks, and passed all 1,182 backend tests at 87.76% coverage.
- An isolated Chromium journey created a device, subnet, interface, assigned IP, and assigned MAC through the browser; reloaded them from Django/PostgreSQL; and then verified their exact ownership and relationships directly in the database.
