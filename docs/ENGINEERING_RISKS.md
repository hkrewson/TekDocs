# Engineering Risk Register

This register turns known limitations into release obligations. A risk remains open until the owning milestone records implementation and verification evidence; a passing scanner or test suite does not close a broader architectural risk.

| ID | Concern | Why it matters | Required solution | Owning milestone(s) |
| --- | --- | --- | --- | --- |
| `TD-RISK-001` | RBAC and client assignment policy requires full-stack certification before client access. | Resolved for the entity foundation in `0.2.0`: one permission/role catalog, complete authenticated-route inventory, method-level Read-only/MFA denials, scoped custom roles and collections, assigned-only reachability, MSP-private audience, cost projection, recovery composition, and cross-tenant/client IDOR suites run as one certification gate. | Keep assignments attached to authenticated tenant memberships rather than Person/job-title records. Every future domain and client-portal route must extend the inventory plus allow, deny, cross-tenant, cross-client, assignment-bypass, field/audience, and privilege-escalation cases before release. | Entity/RBAC foundation certified in `0.2.0`; recurring per domain |
| `TD-RISK-002` | Active RLS must remain complete as the domain expands. | Resolved for the supported one-MSP installation boundary in `0.2.0`: model discovery must match an explicit forced-RLS, authorization-control-plane, or singleton classification. Forced RLS protects domain data; control-plane ownership/edges/actors use scoped managers, central policy, immutable database guards, negative API isolation, and migration-cycle certification. Control-plane reads remain outside RLS to permit tenant establishment and invitation redemption. | Keep the inventory blocking and require every new tenant-owned model to declare its boundary. A hosted multi-MSP topology requires a separately reviewed identity/control-plane isolation design and must not rely on this single-installation certification. | Single-installation foundation certified in `0.2.0`; recurring per model; hosted boundary future |
| `TD-RISK-003` | Browser journeys mostly mock APIs. | Component behavior can pass while cookies, CSRF, proxying, serialization, migrations, or PostgreSQL behavior fail in the real stack. | Maintain at least one isolated, non-mocked browser-to-proxy-to-Django-to-PostgreSQL journey for each delivered critical workflow, starting with organization drill-in. | `0.1.3`, every minor stabilization |
| `TD-RISK-004` | Deployment keys can be exposed through environment/process/operator boundaries. | `0.3.2` mitigates the application/Compose path with mutually exclusive `*_FILE` inputs, approved-root/type/owner/mode/size/content validation, service-scoped mounts, removable bootstrap custody, value/path-free errors, and production rehearsal scans of environments, image history, and logs. The operator-owned 1Password recipe never gives TekDocs a vault token or retrieval capability. Direct environment compatibility remains available, host/container administrators can read runtime material, and zero-downtime rotation/key-loss recovery are not yet implemented. | Keep the file contract and leakage corpus blocking; remove direct sensitive environment fallback from the supported production profile; add explicit service-manager/Kubernetes custody, safe per-key rotation/continuity, and recovery evidence without weakening operator separation. | plan `0.1.3`; file implementation `0.3.2`; production enforcement/hardening `0.8.7`; recovery `0.9.3` |
| `TD-RISK-005` | Local Compose can mix repository provenance. | A fixed project name allowed containers created from the former Documents checkout to be reused by the GitHub checkout, weakening confidence in runtime evidence. | Add a provenance doctor, make isolated rehearsals use unique project names, and recreate ordinary services from the canonical checkout without deleting persistent data. | `0.1.3` |
| `TD-RISK-006` | Supported backup/key recovery must remain rehearsed. | `0.8.1` supplies authenticated encrypted PostgreSQL/media/deployment-key sets, separate recovery-key custody, atomic capture, exact-project destructive confirmation, pre-destruction authentication, runtime-grant restoration, and an independent production-target restore rehearsal. An untested copy, lost recovery key, incomplete remote retention, or unavailable point-in-time recovery can still cause permanent loss. | Keep the full restore rehearsal blocking; add scheduled/off-host retention guidance, deliberate database/media/key-loss and outage exercises, and final recurring recovery evidence. | Initial mitigation `0.8.1`; final recurring closure `0.9.3` |
| `TD-RISK-007` | Runtime evidence must match the production image and custody contract. | The isolated production-target rehearsal now uses file-only database/process/SMTP/OIDC/bootstrap secrets, a one-shot migration service, non-root runtime, least-scope mounts, leakage/ambiguity checks, and fresh volumes. It still builds locally rather than testing a published digest and does not yet enforce read-only filesystems, dropped capabilities, limits, TLS proxy behavior, or graceful shutdown. | Retain the `0.3.2` file-only rehearsal as blocking; add the remaining container/runtime hardening and then execute the same contract against the exact signed release image with SBOM/provenance evidence. | initial rehearsal `0.1.3`; file-custody expansion `0.3.2`; hardening `0.8.7`; release proof `0.9.4` |
| `TD-RISK-008` | Audit evidence must remain append-only below the ORM. | Resolved for the supported PostgreSQL runtime in `0.1.13` and retained through `0.2.0` certification: a row trigger rejects update and delete while inserts remain available, including across migration-cycle testing. Destructive table/schema operations remain in the trusted database-owner boundary. | Retain PostgreSQL bypass-attempt tests and the absence of an application retention bypass. If legal retention deletion is ever required, design a separately reviewed operator mechanism and recertify at stabilization. | Certified in `0.2.0`; recurring |
| `TD-RISK-009` | Reproducible inputs must become signed release provenance. | `0.1.14` adds reviewed hash locks for Python build/production/development dependencies, digest-pins external runtime/tool images, SHA-pins Actions, and locally verifies pin/workflow syntax while retaining Dependabot. The production backend removes pip/setuptools after construction, and vulnerability scans target that production artifact rather than the development image. These controls reproduce and minimize inputs, but do not yet attest a published artifact. | Retain lock/pin drift gates and automated reviewed updates; verify dependency licenses; continue release-image hardening; publish signed exact images, SBOMs, attestations, and provenance. | Input reproducibility implemented in `0.1.14`; hardening `0.8.7`; provenance `0.9.4` |
| `TD-RISK-010` | Hosted GitHub automation is unverified. | Workflows exist, but without a configured remote and observed runs, CodeQL, dependency review, browser matrices, and artifacts are specifications rather than evidence. | Publish/configure the repository when authorized, protect the main branch, require the documented checks, and retain the first successful hosted evidence. | `0.1.14` (externally blocked until publication is authorized) |
| `TD-RISK-011` | The original hand-written router cannot safely scale to nested workspaces. | Dynamic organization routes, history, bookmarks, route errors, and later nested domains would accumulate fragile pathname conditionals. | Adopt a maintained router, central route definitions, explicit not-found behavior, dynamic workspace parameters, accessible links, and route-level tests. | `0.1.3` |
| `TD-RISK-012` | Workspace organization discovery required bounded, permission-filtered search. | Loading every organization would not scale and could disclose more selectable metadata than the user needs. | Resolved for the workspace switcher in `0.1.4` with validated server-side search/pagination/order, scoped results, cancellation/debounce, and negative tests. Apply the same convention API-wide in `0.6.1`. | Resolved in `0.1.4`; API-wide convention in `0.6.1` |
| `TD-RISK-013` | The visual editor remains a large optional dependency. | `0.3.0` certifies the `0.2.9` boundary: the roughly 1.15 MB minified editor remains route-lazy, its styles/syntax assets stay outside the initial shell, and executable 500/1,200 KiB shell/editor budgets block regression. A trial manual split duplicated Crepe/CodeMirror internals and was rejected. Low-powered-device and full-route measurements are not yet complete. | Retain executable budgets and lazy loading; prefer reviewed feature removal or upstream tree-shaking over artificial duplicate chunks; measure representative devices and remediate the editor implementation if the certified budget cannot hold. | Initial mitigation `0.2.9`; documentation certification `0.3.0`; device/load certification `0.8.5` |
| `TD-RISK-014` | Editor/example changes remained outside Git history. | Resolved in `0.2.1`: the supplied UniFi guide, raw-import declaration, editor tests, and browser journey are reviewed parts of the fixed Markdown round-trip corpus. | Keep realistic examples synthetic/non-secret and versioned with the dialect tests. Any future local-only example must be explicitly accepted or excluded before milestone closeout. | Resolved in `0.2.1`; recurring fixture hygiene |
| `TD-RISK-015` | Logs are JSON-shaped rather than safely structured. | Arbitrary process messages can break escaping and later leak or corrupt log ingestion if request/domain values enter message strings. | `0.6.6` adds a separate provider-event ledger limited to fixed codes and numeric metrics, no payload/exception/cursor fields, and 30-day deletion. This does not repair the process-wide formatter: adopt a maintained structured formatter, field allowlist/redaction tests, correlation fields, and failure-path review before production certification. | Provider ledger mitigated `0.6.6`; process-wide closure `0.8.7` |
| `TD-RISK-016` | Local frontend gates depend on an unchecked host Node version. | `make check` can fail before linting when an obsolete host Node is first on `PATH`, even though the project requires Node 24. | Add an explicit runtime doctor or containerized frontend gate so the documented one-command check either uses Node 24 or fails immediately with a precise remedy. | `0.1.3` |
| `TD-RISK-017` | Early performance evidence could drift from authorization and production growth. | `0.3.0` makes the `0.2.9` 2,500-revision PostgreSQL history fixture, bounded pagination, query ceilings, and 500 ms p95 tripwire part of the dedicated documentation certification gate alongside 100 organizations/10,000 unrelated records. This is still not the 250,000-revision capacity target or a concurrent-user/hosted test. | Retain query-count ceilings around authorized services, keep the latency fixture in the release gate, add each new domain family to the dataset, prohibit cross-scope authorization caches, and complete controlled concurrency/search/load evidence on the full 1.0 reference dataset. | Initial baseline `0.1.15`; documentation baseline `0.2.9`; certified foundation `0.3.0`; full load certification `0.8.5` and `0.9.4` |
| `TD-RISK-018` | Schema upgrades can preserve rows while weakening policy or relationship semantics. | A fresh database cannot prove that existing identifiers, archived state, custom-field versions, entity links, role assignments, audit evidence, or forced RLS survive a real migration. `0.8.2` now migrates retained domain fixtures from the stabilized endpoint of every supported prior minor line and requires a tested pre-upgrade recovery point. | Keep the representative PostgreSQL migration cycle and supported-minor matrix beside fresh install; require value-transforming migrations to add preservation assertions and non-reversible migrations to document forward-only recovery. Rollback after migration is full recovery, not arbitrary reversal. | Initial blocking cycle `0.1.15`; full supported-minor matrix `0.8.2`; final platform certification `0.9.2`/`0.9.3` |
| `TD-RISK-019` | The executable Markdown contract, in-app help, and GitHub Wiki can drift apart. | `0.3.0` certifies the blocking local help-contract test beside the fixed round-trip, renderer, sanitizer, and browser accessibility corpus. The public Wiki still does not exist because repository publication has not been authorized. | Treat ADR 0019 plus the fixed fixture as the implementation source of truth; review every dialect change across editor, renderer, sanitizers, tests, and in-app help. Before public beta, publish end-user/operator material to the actual public GitHub Wiki, add link/drift validation, and expose stable contextual-help links without injecting remote executable content. | Initial contract `0.2.1`; local stabilization `0.2.9`; documentation certification `0.3.0`; Wiki/contextual help `0.8.8` |
| `TD-RISK-020` | External credential references can be confused with access grants or unsafe share links. | `0.3.1` stores no customer value, rejects unknown/secret-shaped fields, strictly accepts only canonical 1Password Copy Private Links, labels the record and handoff as a pointer, omits links from list/search responses, separates scoped view/manage/open grants, and records empty-metadata audits. A reference can still become stale after a provider item moves, and strict compatibility relies on an inferred provider grammar because no stable machine-readable grammar is published. `0.3.12` adds bounded pages, archived-pointer denial, lifecycle/performance evidence, and explicit guidance to replace a stale pointer without implying provider validation. | Retain the no-retrieval/no-proxy boundary and fixed negative corpus. Fail closed on provider format changes until deliberate adapter review; do not weaken validation to restore compatibility. Repeat lifecycle, accessibility, upgrade, and restore evidence at inventory releases. | Decision ADR 0020; initial implementation `0.3.1`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0` |
| `TD-RISK-021` | Managed attachments require a durable malware-scanning and quarantine boundary. | `0.3.10` prevents unscanned bytes from becoming managed records: replaceable storage/scanner adapters, opaque quarantine, checksum-verified promotion, clean-only reads, read-time integrity checks, conservative executable/PDF/image/text/ZIP rejection, and a hostile corpus fail closed. `0.3.12` retains checksum-verified managed bytes through a real minor upgrade and clean database/media restore. The built-in structural/content scanner is not equivalent to a maintained malware-signature engine, and external-provider migration, scanner retry/operations, and production engine health still require certification. | Keep downloads forced/private and prohibit inline active-content rendering or archive extraction. Retain the quarantine/clean contract and hostile corpus; add maintained external signature-engine deployment guidance, provider migration, bounded retry/administration, and health/alerting without ever exposing pending or rejected bytes. | Initial bounded contract `0.2.6`; boundary/corpus mitigation `0.3.10`; restore stabilization `0.3.12`; formal certification `0.4.0`; production operations `0.8.7` |
| `TD-RISK-022` | Flexible product specifications can become a validation-denial or forged-provenance boundary. | Arbitrary JSON Schema permits remote resolution, expensive compound evaluation, hostile regular expressions, or ambiguous reinterpretation; matching an asset by supplier/model name would also make provenance mutable and forgeable. | `0.3.3` restricts schemas to bounded closed objects and a maintained non-executable vocabulary, validates values server-side, pins immutable model revisions to immutable definition versions, requires optimistic concurrency, and enforces exact supplier scope. `0.3.4` retains exact supplier/product/model/revision/schema/publication identifiers, validated values, digests, and a checksum; database guards reject relationship forgery, provenance columns/document rows are immutable, and narrow read-only RLS projections expose only supplier catalogs plus associated client-visible STATIC publications to operational workflows. No provenance derives from labels. `0.3.12` proves exact revision, specification version, checksum, and Workspace ownership survive recovery, upgrade, and restore. | Schema/history mitigated `0.3.3`; provenance edge mitigated `0.3.4`; MSP projection `0.3.8`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0` |
| `TD-RISK-023` | Mutable hardware current state can drift from or forge lifecycle history. | A serial, custody target, state, or disposal update performed without a matching event would make operational state and retained evidence disagree; a foreign assignment could also disclose or misattribute a person/location. | `0.3.5` separates one locked current-state profile from an append-only typed ledger, performs mutations transactionally through one service boundary, derives assignment targets from the exact workspace, makes disposal terminal, keeps sensitive free text out of events/audits, and adds null-safe PostgreSQL guards, forced RLS, immutable triggers, and MSP/client/sibling/cross-tenant negative tests. `0.3.12` adds exact archive/recovery comparison, combined scale evidence, and upgrade/restore preservation of serial, tag, state, provenance, and owner. | Initial mitigation `0.3.5`; MSP parity `0.3.8`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0` |
| `TD-RISK-024` | Software entitlement counts or relationships can drift from actual seat allocation. | Concurrent allocation, cross-workspace targets, mismatched products, or mutable history could over-assign a license or create misleading renewal evidence. | `0.3.6` locks licenses during allocation, counts active seats transactionally, requires explicit same-product installation links, validates exact-workspace people/installations, records value-minimized append-only events, and adds null-safe database edge guards plus forced RLS. `0.3.12` proves two competing PostgreSQL transactions cannot exceed a one-seat limit, bounds reference pages, and retains license/seat identity through upgrade and restore. Expiry automation remains later work. | Initial mitigation `0.3.6`; MSP parity `0.3.8`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0` |
| `TD-RISK-025` | Financial values can leak through structurally harmless list/search behavior. | Hiding an amount in detail is insufficient if denied users can infer it from field placeholders, cost counts, totals, sort order, result counts, matching labels/references, validation errors, audit metadata, or authors placing pricing in an operational description. | `0.3.7` stores cost lines separately, projects the entire member only through exact-scope `costs.view`, keeps contract search independent of cost rows, requires `assets.edit` plus cost visibility for mutation, emits value-free audit events, warns authors to keep financial terms in protected rows, and adds denial/sibling tests. `0.3.12` bounds authorized contract counts/pages without consulting cost rows, retains complete-member omission for denied users, excludes costs from CSV, and verifies an authorized cost survives upgrade/restore. Any future aggregate/reporting policy still needs separate oracle review. | Initial mitigation `0.3.7`; MSP parity `0.3.8`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0`; reporting/accounting future review |
| `TD-RISK-026` | MSP operational pages could accidentally become cross-client aggregation surfaces. | A tenant-level query that means “all rows in this tenant” would let an MSP employee infer or read client assets, licenses, providers, contracts, or costs outside their organization assignments. Nullable scope predicates also make ordinary SQL equality fail open or fail inconsistently if guards are not written deliberately. | `0.3.8` added separate MSP routes, central policy enforcement, null-safe operational edges, forced RLS, narrow supplier projections, and two-way isolation tests. `0.3.9` replaces null as the owner identity with the installation's explicit MSP Workspace while retaining null only as its guarded organization-scope projection. Cross-client reporting remains prohibited until a distinct permission-aware projection is designed and certified. | Initial mitigation `0.3.8`; explicit ownership `0.3.9`; inventory stabilization `0.3.12`; formal certification `0.4.0`; every future aggregate/reporting feature |
| `TD-RISK-027` | Nullable organization scope could silently convert an incomplete client create into MSP ownership or leave ownership ambiguous after organization removal. | Treating `organization IS NULL` as both a query predicate and the owner identity cannot distinguish intentional MSP data from an omitted route-derived organization. Deleting an organization could also orphan child rows if ownership foreign keys were cleared or cascaded. | `0.3.9` adds one immutable MSP Workspace per tenant and one immutable Workspace per organization, makes Entity ownership non-null, binds the Workspace UUID into runtime scope, backfills existing Entities, and guards exact tenant/Workspace/organization agreement in PostgreSQL. Organizations remain archived, while protected Workspace and organization references block physical deletion and retain every child owner edge. `Tenant` remains for the supported one-MSP installation and a future hosted seam; it is not a multi-MSP product claim. | Mitigated `0.3.9`; migration/restore recertification `0.3.12`; formal certification `0.4.0`; hosted control plane future review |
| `TD-RISK-028` | Bulk inventory mutation could partially change records or cross an operator's client boundary. | Processing identifiers one at a time before resolving the complete set can commit a prefix, disclose which foreign IDs exist, bypass per-record lifecycle evidence, or leave archived software referenced by active entitlements. | `0.3.10` accepts at most 100 unique IDs, resolves and locks the complete exact Workspace set before mutation, uses one transaction, returns one generic unavailable-selection error, reuses lifecycle/audit services, rejects disposal and active software entitlement dependencies, and makes archive recoverable. Every new bulk action must extend the atomic rollback, permission/IDOR, dependency, and recovery matrix rather than using ORM bulk update directly. | Initial mitigation `0.3.10`; inventory concurrency/load stabilization `0.3.12`; formal certification `0.4.0`; recurring per bulk action |
| `TD-RISK-029` | Spreadsheet transfer can leak sensitive fields, execute formulas, cross ownership boundaries, or create duplicates on retry. | Generic model export tends to expose newly added sensitive columns; permissive mapping weakens validation; CSV cells can become formulas when opened; and an interrupted create batch can be replayed. | `0.3.11` uses a versioned exact-header allowlist with explicit sensitive-domain exclusions, exact-Workspace policy resolution, formula escaping, strict size/encoding/shape/value validation, signed preview-to-apply binding, deterministic Workspace/import-key identity, immutable provenance checks, and one atomic apply transaction. `0.3.12` re-certifies atomic bulk/CSV behavior and preserves retry identity plus provenance across upgrade and restore. Future mapping profiles must translate into this contract and receive separate sample-driven threat review. | Initial mitigation `0.3.11`; stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0`; every future import adapter |
| `TD-RISK-030` | Operational inventory pages can become unbounded, unstable, or accidentally aggregate unauthorized client data. | Full-table responses create memory and browser denial risk; per-row authorization creates N+1 regressions; an MSP-scope shortcut could also turn counts or pages into a cross-client oracle. | `0.3.12` authorizes one exact Workspace before a stable page, caps page size at 100, returns scope-local counts, keeps MSP routes non-aggregate, prefetches related data in fixed queries, and blocks regressions with a 100-client combined PostgreSQL fixture, whole-request query ceilings, p95 limits, IDOR, and forced-RLS composition. | Stabilization ADR 0038 and `0.3.12`; formal certification `0.4.0`; reconsider cursor/indexed search before materially exceeding the documented reference dataset. |
| `TD-RISK-031` | Physical network placement can be forged, overlap under concurrency, or disclose a sibling client's topology. | Validating a unit range only in a form permits direct writes and two concurrent transactions to reserve the same units. Free-form site/rack labels or tenant-wide MSP queries would also weaken ownership and create a topology oracle. | `0.4.1` uses exact-Workspace rack/device Entities, server-resolved active site/location and hardware targets, service row locks, PostgreSQL advisory locks plus range/edge triggers, forced RLS, central network permissions, non-aggregate MSP routes, and sibling/cross-tenant/direct-write/concurrency tests. Rack moves and shrinking fail while placed devices would be contradicted. | Initial mitigation ADR 0040 and `0.4.1`; recovery/scale re-certification `0.4.8`; simplification `0.4.9`; formal network certification `0.5.0` |
| `TD-RISK-032` | Invalid, overlapping, or cross-client IP prefixes can corrupt IPAM and disclose topology. | Form-only CIDR validation misses direct writes; check-then-insert without namespace serialization races. Treating VLANs as routing namespaces can permit accidental overlap, while globally forbidding overlap prevents legitimate address reuse across VRFs. | `0.4.2` stores canonical IPv4/IPv6 prefixes, derives family server-side, defines the default table and each VRF as the only overlap namespaces, and permits reuse only across VRFs. Exact-Workspace resolution, central network permissions, advisory locks, PostgreSQL edge/canonical/overlap guards, forced RLS, property tests, direct-write tests, concurrency tests, and sibling IDOR tests are blocking. | Initial mitigation ADR 0041 and `0.4.2`; address/interface conflict extension `0.4.3`; scale re-certification `0.4.8`; simplification `0.4.9`; formal network certification `0.5.0` |
| `TD-RISK-033` | Address assignments can conflict, cross ownership boundaries, or become invalid when their parent subnet changes. | Form-only checks permit direct or concurrent duplicate IP/MAC records, foreign interface/subnet edges, non-canonical identifiers, and prefix edits that strand existing hosts. A global IP uniqueness rule would also incorrectly forbid legitimate reuse across VRFs. | `0.4.3` resolves every device/subnet/interface in one exact Workspace, requires canonical host and EUI-48 values, rejects IPv4 reserved identifiers, scopes IP conflicts to default/VRF routing namespaces, scopes MAC uniqueness to the Workspace, shares advisory locks with subnet mutation, and adds PostgreSQL containment/edge/conflict triggers, forced RLS, property/concurrency/direct-write/IDOR tests, and non-disclosing API failures. | Initial mitigation ADR 0042 and `0.4.3`; recovery/scale re-certification `0.4.8`; simplification `0.4.9`; formal network certification `0.5.0` |
| `TD-RISK-034` | Wireless documentation can become an unsafe password store, while DNS inventory can expose or corrupt client topology. | Adding credential-shaped fields would duplicate secret custody risk. Tenant-wide DNS reads, foreign IP/VLAN links, non-canonical names, or racing CNAME writes could disclose sibling data or record impossible configurations. | `0.4.5` stores wireless posture but rejects unknown/secret fields and directs access material to external credential references. DNS and wireless records resolve one exact Workspace through central network permissions; type-aware validation, bounded values, same-Workspace edges, advisory locks, PostgreSQL guards, forced RLS, and IDOR/direct-write tests protect topology. TekDocs makes no claim to control Wi-Fi, serve/resolve DNS, or validate live configuration. | Initial mitigation ADR 0043 and `0.4.5`; recovery/scale re-certification `0.4.8`; formal network certification `0.5.0`; live domain/TLS observation `0.7.9`–`0.7.11` |
| `TD-RISK-035` | Circuit provider, contract, and handoff relationships can be forged or become a financial/topology oracle. | A sibling contract or interface identifier could cross a client boundary; contradictory site/device placement can create false operational guidance; concurrent interface reuse can create two claimed demarcations. Exposing contract objects through Network routes can also reveal costs or renewal data to a user who only has network access. | The deferred `0.4.4` scope delivered in build `0.4.6` resolves one exact Workspace, uses central network policy, strict serializers, same-provider/same-Workspace contract validation, interface uniqueness, advisory locks, forced RLS, database edge guards, and sibling/direct-write tests. Contract lifecycle projections require `assets.view`, costs are structurally absent, linked contracts cannot be archived, and reminders remain value-minimized date projections rather than notification jobs. | Initial mitigation ADR 0044 and build `0.4.6`; notification consumption `0.5.4`–`0.5.8`; recovery/scale re-certification `0.4.8`; formal network certification `0.5.0` |
| `TD-RISK-036` | External NetBox identity can be forged, collide, cross a client boundary, or silently turn synchronization destructive. | A remote numeric ID is meaningful only with its type and connection boundary. Tenant-wide mapping, arbitrary remote JSON, or automatic resolution could disclose sibling inventory, retain secrets, overwrite TekDocs, or delete valid records from a partial response. | `0.4.7` binds mappings to exact Workspaces. `0.6.4`–`0.6.7` adds an exact-Workspace read-only NetBox connection, encrypted token, pinned egress, durable page jobs, value-minimized observations, reviewed conflicts, and fingerprint-only acceptance; no provider JSON, delete inference, domain overwrite, or write-back exists. Retain adversarial egress, scale, partial-page, upgrade, and worker-crash tests through certification. | Initial ADR 0045; runtime ADR 0062 and `0.6.4`–`0.6.7`; stabilization `0.6.9`; certification `0.7.0` |
| `TD-RISK-037` | Cross-family network discovery or export can become a client-data oracle, spreadsheet payload, or unbounded memory path. | A tenant-wide union, field-reflective serializer, newly added sensitive column, formula-shaped cell, unstable page, or whole-file buffer could expose sibling records, costs/contracts/provider payloads, execute when opened, duplicate/omit rows, or exhaust the application. | `0.4.8` resolves central `networks.view` and one explicit Workspace first; uses an allowlisted Entity search with bounded text/pages and stable ordering; and streams a versioned, fixed-field/fixed-family CSV in batches. Search omits matched values. Export excludes credential, financial, hardware-only, contract, and provider-payload fields, minimizes NetBox identity, formula-escapes every cell, sets private/no-store headers, and binds an iterator-owned transaction-local RLS scope because Django streams after request middleware returns. Sibling/MSP/anonymous IDOR, a 100-client/10,000-network-Entity PostgreSQL fixture, fixed query ceilings, upgrade, clean restore, and a real production-shaped browser download cover the boundary. Every field/family addition requires an explicit allowlist and oracle review. | Mitigated by ADR 0046 and `0.4.8`; recurring network certification and full load review `0.8.5`/`0.9.4` |
| `TD-RISK-038` | A lightweight network record can duplicate physical identity or destructive simplification can strand retained interface/VRF data. | Allowing new unbacked devices makes placement ownership ambiguous; guessing an asset from a label can forge provenance. Dropping Interface/VRF tables or clearing their edges would break upgrades, exports, and recovery. Direct address assignment can also disclose an asset name to someone who has network but not asset permission. | `0.4.9` requires every new device to extend one exact-Workspace hardware asset in both services and PostgreSQL; legacy unbacked rows receive an explicit non-creatable marker. IP/MAC edges resolve exact hardware assets and project identity only with `assets.view`. Migration backfills only through retained foreign keys, keeps all Interface/VRF rows and compatibility APIs, labels them legacy in search/export, and proves old-data upgrade plus clean restore. Any future table removal needs a separate deprecation and operator-approved migration. | Mitigated by ADR 0047 and `0.4.9`; formal network certification `0.5.0`; compatibility removal not scheduled |
| `TD-RISK-039` | A publication audience label can be mistaken for current client access authority. | If publishing also grants portal visibility, one compromised or mistaken editor can expose retained MSP/client documentation. Mutable approval flags, self-approval, concurrent corrections, or withdrawal-by-delete would weaken both separation of duties and evidence integrity. | `0.5.1` keeps signed snapshots immutable and derives distribution from append-only submitted/approved/withdrawn events. Client-visible approval requires a different MFA-reauthenticated actor with scoped central permission; withdrawal preserves evidence; only active approved client-visible material projects to the future portal. PostgreSQL enforces event order, actor separation, exact scope, one approved successor, immutability, and forced RLS under advisory locks. Review the historical-approval backfill before portal enablement and extend the same projection/IDOR matrix through every client session and notification path. | Initial mitigation ADR 0049 and `0.5.1`; portal boundary `0.5.2`–`0.5.3`; notification payloads `0.5.4`–`0.5.7`; certification `0.6.0` |
| `TD-RISK-040` | A client identity can be mistaken for an ordinary MSP tenant member or retargeted to another organization. | Reusing general tenant roles, route-selected organizations, or the MSP workspace shell could let a client enumerate organizations or call staff APIs even when content queries are individually scoped. A mutable membership edge would turn an account-management defect into cross-client access. | `0.5.2` gives client memberships one immutable active-client organization, declares an explicit portal session surface, rejects client roles at the central MSP policy boundary, derives portal scope only from membership, renders a separate browser shell, and repeats role/scope/tenant/classification/immutability checks in PostgreSQL. Extend the portal-only IDOR matrix with every content and notification endpoint. | Initial mitigation ADR 0050 and `0.5.2`; content projection `0.5.3`; notification payloads `0.5.4`–`0.5.7`; certification `0.6.0` |
| `TD-RISK-041` | A client-visible publication can retain reference-card text resolved with MSP permissions. | Approval of a document does not automatically authorize every embedded record, while historical STATIC snapshots cannot be edited safely. | `0.5.3` rejects new approval unless every referenced Entity is explicitly client-visible in the exact organization. The portal repeats the check and omits unsafe historical projections without altering evidence. MSP listing references remain non-authorizing until a dedicated per-client projection exists. | Initial mitigation ADR 0051 and `0.5.3`; recurring portal certification through `0.6.0` |
| `TD-RISK-042` | Notification side effects can be lost, duplicated, cross-scoped, or become a secondary sensitive-data store. | Sending inside a request can commit the domain change after delivery fails, while sending before commit can announce rolled-back state. Retrying without stable identity can duplicate delivery, and arbitrary payload/error serialization can retain emails, document content, reasons, credentials, or exception values. A retained notification label can also outlive the permission that originally allowed it. | `0.5.4` writes fixed-schema, value-minimized events in the domain transaction and supplies bounded idempotent delivery. `0.5.5` stores immutable recipient edges and re-authorizes display. `0.5.6` attaches a separate idempotent SMTP queue, resolves addresses only in memory, repeats authorization and preference checks at send time, renders fixed generic templates, retains only safe status codes, retries temporary outages, and suppresses revoked access. `0.5.7` batches only one recipient/surface, re-authorizes every item, defers schedules without attempt inflation, exposes only MFA-gated metadata, and requires audited generated dead-letter replay. `0.5.9` adds scope-bound signed seek cursors, bounded grouped read authorization, large-history query/latency budgets, and consolidated upgrade/restore evidence. PostgreSQL guards identity/transitions/preferences with forced RLS. SMTP remains at-least-once across a crash after remote acceptance, and asynchronous provider bounces are not ingested. | Initial mitigation ADR 0052 and `0.5.4`; inbox mitigation ADR 0053 and `0.5.5`; SMTP mitigation ADR 0054 and `0.5.6`; scheduling/admin ADR 0055 and `0.5.7`; history/load mitigation ADR 0056 and `0.5.9`; certification `0.6.0` |
| `TD-RISK-043` | An over-modeled network surface can become a competing partial NetBox and duplicate physical identity. | Exposing schema families as navigation makes routine documentation require host-by-host IPAM, encourages conflicting rack/device records outside Assets, and presents manual remote identifiers without a configured integration authority. Destructive simplification could also strand already-certified rows and stable references. | `0.5.10` exposes one Network record with derived gateway/range, moves MAC authoring under exact-Workspace hardware Assets, removes granular and NetBox sections from ordinary navigation, and retains every older table/API/ID behind compatibility boundaries. PostgreSQL continues to guard canonical CIDR, overlap, location scope, and explicit ranges. Any compatibility removal requires a separate deprecation and upgrade/restore plan; NetBox returns only through the integration provider contract. | Mitigated by ADR 0057 and `0.5.10`; integration ownership `0.6.4`–`0.6.7`; compatibility removal not scheduled |
| `TD-RISK-044` | Generated-client names can drift when OpenAPI operation IDs collide. | Generator-order suffixes can rename an unrelated operation when a neighboring route is added. | `0.6.9` replaces the 30 ambiguous list/detail IDs through a narrow schema policy that derives stable list/detail names while preserving scope prefixes; uniqueness and checked-client drift are blocking tests. `0.7.0` composes this with the complete API/integration certification gate. Treat future operation-ID changes as reviewed API compatibility events. | Resolved `0.6.9`; certified `0.7.0`; recurring API compatibility review |
| `TD-RISK-045` | An idempotency header can be mistaken for durable replay protection. | Echoing a syntactically valid key does not bind it to a request body or retain its result. Advertising that behavior on arbitrary creates could duplicate side effects or let one key be reused with different semantics. | `0.6.1` declares the header only on naturally convergent operations and documents correlation separately from replay. `0.6.5` gives integration jobs an exact-connection key, request fingerprint, retained state/result, and collision rejection; `0.6.9` proves retry/lease behavior and `0.7.0` composes the contract. New mutation families must opt in only with equivalent durable semantics. | Certified `0.7.0`; recurring API mutation review |
| `TD-RISK-046` | Long-lived bearer credentials can bypass browser MFA/CSRF and become a client-data exfiltration path. | A copied token is sufficient until expiry; a service identity or scope bug could turn one leaked credential into tenant-wide authority, while plaintext retention would make a database disclosure immediately exploitable. | `0.6.2` stores only maintained password hashes, reveals material once, requires MFA-backed recent session issuance, bounds expiry, uses exact Workspace plus explicit permission intersections, excludes MFA/browser-only grants, creates non-interactive read-only service identities, rotates/revokes atomically, and guards authority in PostgreSQL. `0.7.0` certifies the implemented exact-Workspace boundary. TLS remains mandatory; usage-anomaly detection and operator-scale administration remain pre-1.0 operational owners rather than part of this claim. | Certified `0.7.0`; anomaly/administration owner `0.8.7` |
| `TD-RISK-047` | Webhooks and provider connectors can become an SSRF, replay, cross-client disclosure, forged-event, or unbounded retry channel. | A configurable destination can probe private infrastructure or change DNS between validation and connect; retained bodies/errors can become a second sensitive-data store; broad subscriptions or cursors can cross client/origin boundaries. | `0.6.3` establishes signed value-minimized webhooks and `0.6.4`–`0.6.8` establishes read-only providers. `0.6.9` adds adversarial cursor/redirect/content/DNS/size tests, control-character rejection, transactional page rollback, lease recovery, scale, and exact-prior upgrade/restore. `0.7.0` certifies the composed boundary; proxy behavior and final DAST recur at `0.8.7`. | Certified `0.7.0`; recurring DAST `0.8.7` |
| `TD-RISK-048` | Compliance catalog edits can silently rewrite historical meaning, forge control identity, or cross a client boundary. | In-place text updates invalidate prior reviews/evidence; copied catalogs without stable identities make reuse ambiguous; tenant-wide lookups or forged parent edges can disclose or attribute a client's controls to another Workspace. | `0.7.1` separates stable Entity-backed framework/control identity from immutable content revisions, stores each catalog as an ordered snapshot of exact control revisions, calculates canonical digests, resolves one explicit Workspace through central compliance policy, rejects sibling IDs, and adds PostgreSQL scope/actor/entity/parent/duplicate/retention guards plus forced RLS. Catalogs remain documentation, not proof of applicability or certification; assignment/evidence semantics and signed evidence bundles are later slices. | Initial mitigation ADR 0063 and `0.7.1`; assignments `0.7.2`; evidence/bundles `0.7.3`–`0.7.5`; certification `0.8.0` |
| `TD-RISK-049` | Compliance assignment can become an access grant, stale owner edge, or rewritten review claim. | Free-form people or unchecked user IDs can assign accountability across clients; mutable history can make old decisions appear to cover new control wording. | `0.7.2` keeps one exact-Workspace current assignment separate from append-only reviews, pins every review to an exact control revision, and permits only active TekDocs users who independently retain exact-scope `compliance.view`. Central policy, non-disclosing APIs, PostgreSQL edge/identity/retention guards, forced RLS, and negative IDOR tests repeat the boundary. Assignment never grants access and does not establish certification. | Initial mitigation ADR 0064 and `0.7.2`; evidence/risk composition `0.7.3`–`0.7.5`; certification `0.8.0` |
| `TD-RISK-050` | Reusable compliance evidence can cross a client boundary or make a later control revision appear previously reviewed. | Unscoped reuse discloses client material; mutable links or reviews can rewrite provenance; one deduplicated edge can falsely cover changed control wording. | `0.7.3` gives evidence an Entity-backed exact-Workspace identity, validates source/assignment/actor/window edges, and stores append-only links pinned to exact control revisions plus append-only reviews. A later revision creates another retained link. Central policy, non-disclosing APIs, PostgreSQL guards, forced RLS, and negative IDOR tests preserve scope. Evidence remains documentation, not certification. | Initial mitigation ADR 0065 and `0.7.3`; bundle composition `0.7.5`; certification `0.8.0` |
| `TD-RISK-051` | Risk scores or acceptance can be forged, silently rewritten, or disclosed across clients. | Free-form severity creates inconsistent reporting; mutable decisions erase accountability; unchecked owners/control edges create cross-client attribution; treatment text can be mistaken for formal acceptance. | `0.7.4` bounds likelihood/impact and derives score/band server-side, reauthorizes owners and exact-Workspace assignments, requires matching accepted status/treatment, records accepting actor/time, and appends full-state decisions pinned to exact control revisions. Database guards, forced RLS, non-disclosing APIs, and scoped summaries repeat the boundary. | Initial mitigation ADR 0066 and `0.7.4`; bundle composition `0.7.5`; certification `0.8.0` |
| `TD-RISK-052` | A compliance export can omit dependencies, cross scope, or be altered after review. | Mutable or noncanonical exports undermine evidence integrity and can leak sibling data. | `0.7.5` resolves only one authorized Workspace, serializes exact revisions/links/windows/outcomes canonically, signs SHA-256 with the deployment Ed25519 key, retains public verification material, and enforces database immutability plus forced RLS. | Initial mitigation ADR 0067 and `0.7.5`; certification `0.8.0` |
| `TD-RISK-053` | A reminder or calendar feed can disclose hidden client deadlines or become a long-lived bearer secret. | Cross-client aggregation, guessed source IDs, or public feed tokens can reveal client relationships and operational timing after access is revoked. | `0.7.6` keeps schedules in one exact Workspace, revalidates source type and current owner authority, adds dedicated central permissions, forces RLS, and exports only through the authenticated session with private/no-store headers and a 5,000-event bound. Public subscription tokens and notification generation remain disabled until separately designed. | Initial mitigation ADR 0068 and `0.7.6`; notification integration `0.7.10`; certification `0.8.0` |
| `TD-RISK-054` | Entered domain ownership can cross clients, be confused with discovered truth, or expose renewal timing. | A tenant-wide list or silently overwritten registration date can disclose customers and produce false operational confidence. | `0.7.8` gives each normalized domain an exact Workspace Entity, dedicated permissions, authorized registrar/owner edges, non-aggregating APIs, database guards, and forced RLS. Entered fields remain explicitly separate from future observations. | Initial mitigation ADR 0070 and `0.7.8`; observations `0.7.9`–`0.7.11`; certification `0.8.0` |
| `TD-RISK-055` | Certificate monitoring can become an internal-network probe, retain sensitive certificate payloads, or report misleading validation. | User-controlled hosts or ports can target internal services; DNS can change between validation and connection; using one permissive handshake can confuse reachability, hostname identity, and public CA trust. Retained PEM/DER or exception text can also become a secondary sensitive-data store. | `0.7.12` permits only exact-domain/hostname fixed-port direct TLS, rejects IP literals and every resolution containing a non-public address, pins the reviewed address while preserving authored SNI, requires TLS 1.2+, and records hostname and CA trust independently. Raw certificates and exception values are discarded; only bounded digests/metadata remain. Central policy, exact-Workspace workers, PostgreSQL identity/retention guards, forced RLS, and hostile-resolution tests repeat the boundary. | Initial mitigation ADR 0074 and `0.7.12`; IDN/wildcard/SAN, scale, isolation, upgrade, and evidence-integrity stabilization `0.7.13`; certification `0.8.0` |
| `TD-RISK-056` | Critical workflows can be visually available while remaining unusable or ambiguous to keyboard and assistive-technology users. | Missing bypass navigation, unmanaged route focus, incomplete composite-widget semantics, obscured focus, low contrast, motion, or color-only state can block authentication, editing, publication, portal, and administrative work. Automated scanners alone cannot establish conformance. | `0.8.3` establishes a WCAG 2.2 AA engineering contract, remediates shell/menu/editor focus and keyboard behavior plus contrast/preference states, and adds explicit WCAG-tagged automated regressions. Manual keyboard, VoiceOver, reflow/zoom, text-spacing, and contrast-mode review remains blocking release evidence. | Initial mitigation ADR 0079 and `0.8.3`; browser matrix `0.8.6`; external review `0.8.9`; final candidate `0.9.5` |
| `TD-RISK-057` | Locale or timezone assumptions can shift deadlines, create ambiguous timestamps, or make translated interfaces structurally unusable. | Host-local defaults, offset-free timestamps, parsing date-only values as instants, partial catalogs, sentence fragments, and missing direction/plural review can silently change operational meaning. | `0.8.4` defaults servers to UTC, validates production IANA zones, keeps workflow zones explicit, rejects ambiguous browser timestamps, formats plain dates without zone shifts, and establishes a shipped-catalog-only typed presentation seam. A locale requires complete reviewed browser/mail/PDF/accessibility evidence before advertisement. | Initial mitigation ADR 0080 and `0.8.4`; browser matrix `0.8.6`; documentation `0.8.8`; final candidate `0.9.5` |

## 0.5.0 network certification disposition

`TD-RISK-031` through `TD-RISK-038` are **mitigated with recurring owners** at the `0.5.0` boundary. None is marked resolved: exact-Workspace policy, forced RLS, database guards, bounded projections, concurrency coverage, upgrade/restore evidence, and the named network certification gate remain required. NetBox connector authority and any destructive Interface/VRF compatibility removal remain assigned to later, separately reviewed work.

## 0.5.1 publication-control disposition

`TD-RISK-039` is **mitigated with recurring owner**. The decision ledger and audience projection are certified only for authenticated MSP-side management; no client session, portal read, notification delivery, or public access exists yet. Those later slices must consume the projection rather than infer access from `audience` or publication existence.

## 0.5.2 client-identity disposition

`TD-RISK-040` is **mitigated with recurring owner**. Exact-client identity and the portal/MSP policy split are enforced, but every later portal resource and event projection must retain the same membership-derived scope and negative matrix.

## 0.5.3 client-publication disposition

`TD-RISK-041` is **mitigated with recurring owner**. Direct organization publications have a fail-closed reference projection. MSP-owned reference documents remain intentionally unavailable until an explicit immutable per-client projection and approval contract is designed.

## 0.5.4 transactional-delivery disposition

`TD-RISK-042` is **mitigated with recurring owner**. Domain events now survive transaction and worker failures without accepting arbitrary payloads, but the internal receipt is not yet a user notification or SMTP delivery. Every later consumer must re-authorize recipients and retain event-ID idempotency.

## 0.5.5 inbox-projection disposition

`TD-RISK-042` remains **mitigated with recurring owner**. In-app and SMTP paths repeat permission-filtered projections and retain no destination or rendered content. Bounded digests, quiet-time deferral, metadata-only administration, and audited retry are delivered in `0.5.7`.

## 0.5.9 portal/notification stabilization disposition

`TD-RISK-003`, `TD-RISK-039`, `TD-RISK-040`, `TD-RISK-041`, and `TD-RISK-042` remain **mitigated with recurring owners**. Signed scope-bound history cursors, grouped permission reauthorization, bounded scan/page sizes, PostgreSQL load fixtures, accessibility behavior, exact-prior upgrade, and independent database/media restore remove the known fixed-window and N+1 weaknesses. Full browser-matrix certification remains `0.6.0`; asynchronous provider delivery-status ingestion remains part of later integration work.

## 0.5.10 simple-network disposition

`TD-RISK-031` through `TD-RISK-038` remain protected by their existing database and isolation controls, while `TD-RISK-043` is **mitigated with recurring owner**. The ordinary UI no longer promotes legacy granular authoring, but the retained compatibility API is still real product debt. It must not be removed or widened without explicit migration, export, permission, and recovery review.

## 0.6.0 controlled-client-access certification disposition

`TD-RISK-003`, `TD-RISK-039`, `TD-RISK-040`, `TD-RISK-041`, and `TD-RISK-042` are **certified for the implemented one-MSP installation boundary with recurring owners**. The named PostgreSQL composition, real publisher/approver/client browser journey, exact-`0.5.9` upgrade, independent PostgreSQL/media restore, and production-image checks are now one release contract. This does not close SMTP's remote-acceptance crash window, provider bounce ingestion, supported encrypted/key-loss recovery, hosted workflow evidence, anonymous access, client co-authoring, or hosted multi-MSP isolation.

## 0.7.2 compliance-assignment disposition

`TD-RISK-048` and `TD-RISK-049` are **mitigated with recurring owners**. Exact control revisions, owner reauthorization, append-only reviews, database guards, forced RLS, and Workspace IDOR checks protect the implemented assignment boundary. Evidence linkage, risk treatment, signed bundles, full upgrade/restore composition, and compliance certification remain owned by `0.7.3`–`0.8.0`.

## 0.7.3 compliance-evidence disposition

`TD-RISK-048` through `TD-RISK-050` are **mitigated with recurring owners**. Exact-Workspace evidence identity, exact-revision retained links, append-only decisions, database guards, forced RLS, and sibling IDOR checks protect the implemented reuse boundary. Risk composition, signed bundles, full upgrade/restore composition, and compliance certification remain owned by `0.7.4`–`0.8.0`.

## 0.7.4 compliance-risk disposition

`TD-RISK-048` through `TD-RISK-051` are **mitigated with recurring owners**. Derived scoring, explicit acceptance, current owner authorization, exact-control linkage, append-only decisions, database guards, forced RLS, and non-aggregating reports protect the implemented risk boundary. Signed bundles, full upgrade/restore composition, and compliance certification remain owned by `0.7.5`–`0.8.0`.

## 0.7.5 signed-evidence-bundle disposition

`TD-RISK-048` through `TD-RISK-052` are **mitigated with recurring owners**. Exact-Workspace canonical manifests retain control revisions, evidence links and reviews, and risk decisions; SHA-256 and Ed25519 provide offline integrity evidence. Database immutability, forced RLS, bounded bundle size, and non-disclosing APIs protect retention and isolation. Full upgrade/restore composition and compliance certification remain owned by `0.8.0`.

## 0.7.6 shared-reminder disposition

`TD-RISK-053` is **mitigated with recurring owner**. Exact-Workspace source validation, dedicated policy grants, database identity guards, forced RLS, stable calendar UIDs, event bounds, and authenticated private delivery protect the implemented schedule boundary. Notification emission and any revocable public subscription design remain owned by later milestones.

## 0.7.7 approved-egress disposition

`TD-RISK-047` remains **mitigated with recurring owner**. One reusable service now owns hostname normalization, every-answer public DNS validation, address pinning, and TLS hostname/SNI preservation for existing webhook and provider callers. Each caller retains explicit no-redirect, timeout, body, content, and origin constraints. Proxy behavior, DAST, and every future collector's adoption remain recurring owners through `0.8.7`.

## 0.7.8 registered-domain disposition

`TD-RISK-054` is **mitigated with recurring owner**. Stable IDNA ASCII identity, exact-Workspace ownership, dedicated policy, authorized relationships, non-aggregating APIs, database guards, and forced RLS protect entered registration records. Discovered truth, reconciliation, and expiry notification remain later boundaries.

## 0.7.9 domain-hierarchy disposition

`TD-RISK-054` remains **mitigated with recurring owner**. Exact-domain hierarchy, active-name uniqueness, explicit provenance, append-only digested observations, database scope/retention guards, and forced RLS protect the manual observation boundary. Automated collection and reconciliation remain `0.7.11`.

## 0.7.10 domain-renewal disposition

`TD-RISK-053` and `TD-RISK-054` remain **mitigated with recurring owners**. Domain expiration creates an exact-source schedule, review events retain entered/observed differences, and the authenticated calendar consumes the shared scoped projection. Direct request-path notification is prohibited; the later reminder dispatcher owns outbox integration.

## 0.7.11 domain-monitoring disposition

`TD-RISK-047`, `TD-RISK-053`, and `TD-RISK-054` remain **mitigated with recurring owners**. RDAP and DNS-over-HTTPS collection use the shared pinned public-HTTPS policy with bounded responses and no redirects; remote bodies are digested then discarded. Exact-Workspace jobs, observations, automated reviews, and alerts use forced RLS and database scope/immutability guards, while entered registration truth remains separate. The configured recursive resolver is explicit provenance rather than a claim of direct authoritative access. TLS monitoring and the full monitoring certification remain owned by `0.7.12`–`0.8.0`; general inbox/SMTP recipient projection for monitoring alerts remains a separately reviewed extension.

## 0.7.12 certificate-monitoring disposition

`TD-RISK-047`, `TD-RISK-054`, and `TD-RISK-055` are **mitigated with recurring owners**. Fixed protocol ports, every-answer public-address validation, address pinning, authored SNI, TLS 1.2+, separate hostname/trust results, reduced retained evidence, exact-Workspace workers, database guards, forced RLS, and negative tests establish the initial certificate-monitoring boundary. `0.7.13` still owns expanded IDN/wildcard/SAN evidence, scale/accessibility/isolation stabilization, and upgrade/restore proof; `0.8.0` owns composed certification. General inbox/SMTP projection remains excluded until recipient routing is separately authorized.

## 0.7.13 monitoring-stabilization disposition

`TD-RISK-047`, `TD-RISK-053`, `TD-RISK-054`, and `TD-RISK-055` are **mitigated with recurring owners**. Strict IDNA and wildcard handling, typed DoH success validation, explicit DNSSEC/CAA summaries, bounded certificate/SAN evidence, canonical terminal-run digests, database retention guards, bounded history, accessible controls, exact-Workspace regression, and exact-prior upgrade/restore evidence close the stabilization slice. `0.8.0` owns composed compliance-and-monitoring certification. General inbox/SMTP projection remains excluded until recipient routing is separately authorized.

## 0.8.0 compliance-and-monitoring certification disposition

`TD-RISK-047` and `TD-RISK-048` through `TD-RISK-055` are **certified for the implemented one-MSP, exact-Workspace boundary with recurring owners**. The named PostgreSQL composition, real browser-to-Django-to-PostgreSQL journey, exact-`0.7.13` upgrade, independent PostgreSQL/media restore, and production-target secret-file/image rehearsal now form one release contract. This does not close supported encrypted/key-loss recovery (`0.8.1`/`0.9.3`), proxy-aware DAST (`0.8.7`), external security review (`0.8.9`), host/database administrator trust, or general inbox/SMTP routing for monitoring alerts.

## 0.8.1 encrypted recovery disposition

`TD-RISK-006` is **mitigated with a recurring owner** for operator-invoked full recovery. AES-256-GCM artifact authentication, HMAC-authenticated metadata, separate recovery-key custody, no plaintext host staging, exact-project confirmation, archive allowlisting, restored runtime grants, and independent production-target state verification form the supported boundary. Scheduling, remote retention, point-in-time recovery, and deliberate lost-key/outage exercises remain `0.9.3` obligations.

## 0.8.2 supported-upgrade disposition

`TD-RISK-018` is **mitigated with recurring owners** across the declared source matrix. Retained identity, document/publication, inventory, network, portal/notification, integration, and compliance/monitoring fixtures migrate from each stabilized prior minor line into the current owner/runtime-role boundary. Platform breadth remains `0.9.2`; outage and final recovery composition remain `0.9.3`.

## 0.8.3 accessibility disposition

`TD-RISK-056` is **mitigated with recurring owners**. Bypass navigation, route focus, menu focus return, editor composite semantics, contrast, reduced motion, forced colors, and WCAG 2.2-tagged regression checks protect the implemented critical surfaces. Automation is not represented as certification; full engine/device evidence, external review, and final candidate review remain `0.8.6`, `0.8.9`, and `0.9.5` obligations.

## 0.8.4 localization/time disposition

`TD-RISK-057` is **mitigated with recurring owners**. UTC server defaults, IANA production validation, explicit civil-schedule zones, offset-aware instant formatting, stable plain dates, shipped-catalog negotiation, and typed shared messages establish the boundary. Only `en-US` is supported; feature-copy migration and full browser/mail/PDF/accessibility evidence remain required before another locale is advertised.

## Release rule

Release evidence must name every risk assigned to that milestone and record it as `resolved`, `mitigated with remaining owner`, or `blocked`. Moving a risk requires updating both this register and the destination milestone; it may not disappear from a release note merely because feature work passed.
