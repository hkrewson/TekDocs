# Engineering Risk Register

This register turns known limitations into release obligations. A risk remains open until the owning milestone records implementation and verification evidence; a passing scanner or test suite does not close a broader architectural risk.

| ID | Concern | Why it matters | Required solution | Owning milestone(s) |
| --- | --- | --- | --- | --- |
| `TD-RISK-001` | RBAC and client assignment policy requires full-stack certification before client access. | Resolved for the entity foundation in `0.2.0`: one permission/role catalog, complete authenticated-route inventory, method-level Read-only/MFA denials, scoped custom roles and collections, assigned-only reachability, MSP-private audience, cost projection, recovery composition, and cross-tenant/client IDOR suites run as one certification gate. | Keep assignments attached to authenticated tenant memberships rather than Person/job-title records. Every future domain and client-portal route must extend the inventory plus allow, deny, cross-tenant, cross-client, assignment-bypass, field/audience, and privilege-escalation cases before release. | Entity/RBAC foundation certified in `0.2.0`; recurring per domain |
| `TD-RISK-002` | Active RLS must remain complete as the domain expands. | Resolved for the supported one-MSP installation boundary in `0.2.0`: model discovery must match an explicit forced-RLS, authorization-control-plane, or singleton classification. Forced RLS protects domain data; control-plane ownership/edges/actors use scoped managers, central policy, immutable database guards, negative API isolation, and migration-cycle certification. Control-plane reads remain outside RLS to permit tenant establishment and invitation redemption. | Keep the inventory blocking and require every new tenant-owned model to declare its boundary. A hosted multi-MSP topology requires a separately reviewed identity/control-plane isolation design and must not rely on this single-installation certification. | Single-installation foundation certified in `0.2.0`; recurring per model; hosted boundary future |
| `TD-RISK-003` | Browser journeys mostly mock APIs. | Component behavior can pass while cookies, CSRF, proxying, serialization, migrations, or PostgreSQL behavior fail in the real stack. | Maintain at least one isolated, non-mocked browser-to-proxy-to-Django-to-PostgreSQL journey for each delivered critical workflow, starting with organization drill-in. | `0.1.3`, every minor stabilization |
| `TD-RISK-004` | Deployment keys can be exposed through environment/process/operator boundaries. | `0.3.2` mitigates the application/Compose path with mutually exclusive `*_FILE` inputs, approved-root/type/owner/mode/size/content validation, service-scoped mounts, removable bootstrap custody, value/path-free errors, and production rehearsal scans of environments, image history, and logs. The operator-owned 1Password recipe never gives TekDocs a vault token or retrieval capability. Direct environment compatibility remains available, host/container administrators can read runtime material, and zero-downtime rotation/key-loss recovery are not yet implemented. | Keep the file contract and leakage corpus blocking; remove direct sensitive environment fallback from the supported production profile; add explicit service-manager/Kubernetes custody, safe per-key rotation/continuity, and recovery evidence without weakening operator separation. | plan `0.1.3`; file implementation `0.3.2`; production enforcement/hardening `0.8.7`; recovery `0.9.3` |
| `TD-RISK-005` | Local Compose can mix repository provenance. | A fixed project name allowed containers created from the former Documents checkout to be reused by the GitHub checkout, weakening confidence in runtime evidence. | Add a provenance doctor, make isolated rehearsals use unique project names, and recreate ordinary services from the canonical checkout without deleting persistent data. | `0.1.3` |
| `TD-RISK-006` | Supported backup/key recovery does not exist. | Loss of PostgreSQL, attachment volumes, or wrapping keys can permanently lose data or encrypted MFA/secret material. | Build encrypted backup/restore tooling, separate key custody, destructive-operation safeguards, integrity verification, and recurring restore/database/key-loss rehearsals. | `0.8.1`, `0.9.3` |
| `TD-RISK-007` | Runtime evidence must match the production image and custody contract. | The isolated production-target rehearsal now uses file-only database/process/SMTP/OIDC/bootstrap secrets, a one-shot migration service, non-root runtime, least-scope mounts, leakage/ambiguity checks, and fresh volumes. It still builds locally rather than testing a published digest and does not yet enforce read-only filesystems, dropped capabilities, limits, TLS proxy behavior, or graceful shutdown. | Retain the `0.3.2` file-only rehearsal as blocking; add the remaining container/runtime hardening and then execute the same contract against the exact signed release image with SBOM/provenance evidence. | initial rehearsal `0.1.3`; file-custody expansion `0.3.2`; hardening `0.8.7`; release proof `0.9.4` |
| `TD-RISK-008` | Audit evidence must remain append-only below the ORM. | Resolved for the supported PostgreSQL runtime in `0.1.13` and retained through `0.2.0` certification: a row trigger rejects update and delete while inserts remain available, including across migration-cycle testing. Destructive table/schema operations remain in the trusted database-owner boundary. | Retain PostgreSQL bypass-attempt tests and the absence of an application retention bypass. If legal retention deletion is ever required, design a separately reviewed operator mechanism and recertify at stabilization. | Certified in `0.2.0`; recurring |
| `TD-RISK-009` | Reproducible inputs must become signed release provenance. | `0.1.14` adds reviewed hash locks for Python build/production/development dependencies, digest-pins external runtime/tool images, SHA-pins Actions, and locally verifies pin/workflow syntax while retaining Dependabot. The production backend removes pip/setuptools after construction, and vulnerability scans target that production artifact rather than the development image. These controls reproduce and minimize inputs, but do not yet attest a published artifact. | Retain lock/pin drift gates and automated reviewed updates; verify dependency licenses; continue release-image hardening; publish signed exact images, SBOMs, attestations, and provenance. | Input reproducibility implemented in `0.1.14`; hardening `0.8.7`; provenance `0.9.4` |
| `TD-RISK-010` | Hosted GitHub automation is unverified. | Workflows exist, but without a configured remote and observed runs, CodeQL, dependency review, browser matrices, and artifacts are specifications rather than evidence. | Publish/configure the repository when authorized, protect the main branch, require the documented checks, and retain the first successful hosted evidence. | `0.1.14` (externally blocked until publication is authorized) |
| `TD-RISK-011` | The original hand-written router cannot safely scale to nested workspaces. | Dynamic organization routes, history, bookmarks, route errors, and later nested domains would accumulate fragile pathname conditionals. | Adopt a maintained router, central route definitions, explicit not-found behavior, dynamic workspace parameters, accessible links, and route-level tests. | `0.1.3` |
| `TD-RISK-012` | Workspace organization discovery required bounded, permission-filtered search. | Loading every organization would not scale and could disclose more selectable metadata than the user needs. | Resolved for the workspace switcher in `0.1.4` with validated server-side search/pagination/order, scoped results, cancellation/debounce, and negative tests. Apply the same convention API-wide in `0.6.1`. | Resolved in `0.1.4`; API-wide convention in `0.6.1` |
| `TD-RISK-013` | The visual editor remains a large optional dependency. | `0.3.0` certifies the `0.2.9` boundary: the roughly 1.15 MB minified editor remains route-lazy, its styles/syntax assets stay outside the initial shell, and executable 500/1,200 KiB shell/editor budgets block regression. A trial manual split duplicated Crepe/CodeMirror internals and was rejected. Low-powered-device and full-route measurements are not yet complete. | Retain executable budgets and lazy loading; prefer reviewed feature removal or upstream tree-shaking over artificial duplicate chunks; measure representative devices and remediate the editor implementation if the certified budget cannot hold. | Initial mitigation `0.2.9`; documentation certification `0.3.0`; device/load certification `0.8.5` |
| `TD-RISK-014` | Editor/example changes remained outside Git history. | Resolved in `0.2.1`: the supplied UniFi guide, raw-import declaration, editor tests, and browser journey are reviewed parts of the fixed Markdown round-trip corpus. | Keep realistic examples synthetic/non-secret and versioned with the dialect tests. Any future local-only example must be explicitly accepted or excluded before milestone closeout. | Resolved in `0.2.1`; recurring fixture hygiene |
| `TD-RISK-015` | Logs are JSON-shaped rather than safely structured. | Arbitrary messages can break escaping and later leak or corrupt log ingestion if request/domain values enter message strings. | Adopt a maintained structured formatter, field allowlist/redaction tests, correlation fields, bounded retention, and failure-path review. | `0.6.6`, `0.8.7` |
| `TD-RISK-016` | Local frontend gates depend on an unchecked host Node version. | `make check` can fail before linting when an obsolete host Node is first on `PATH`, even though the project requires Node 24. | Add an explicit runtime doctor or containerized frontend gate so the documented one-command check either uses Node 24 or fails immediately with a precise remedy. | `0.1.3` |
| `TD-RISK-017` | Early performance evidence could drift from authorization and production growth. | `0.3.0` makes the `0.2.9` 2,500-revision PostgreSQL history fixture, bounded pagination, query ceilings, and 500 ms p95 tripwire part of the dedicated documentation certification gate alongside 100 organizations/10,000 unrelated records. This is still not the 250,000-revision capacity target or a concurrent-user/hosted test. | Retain query-count ceilings around authorized services, keep the latency fixture in the release gate, add each new domain family to the dataset, prohibit cross-scope authorization caches, and complete controlled concurrency/search/load evidence on the full 1.0 reference dataset. | Initial baseline `0.1.15`; documentation baseline `0.2.9`; certified foundation `0.3.0`; full load certification `0.8.5` and `0.9.4` |
| `TD-RISK-018` | Schema upgrades can preserve rows while weakening policy or relationship semantics. | A fresh database cannot prove that existing identifiers, archived state, custom-field versions, entity links, role assignments, audit evidence, or forced RLS survive a real migration. | Keep a representative PostgreSQL migration-cycle test beside fresh-install and oldest-supported upgrade rehearsals; require value-transforming migrations to add preservation assertions and require non-reversible migrations to document a forward-only recovery procedure. | Initial blocking cycle `0.1.15`; every schema milestone and final `0.9.3` upgrade certification |
| `TD-RISK-019` | The executable Markdown contract, in-app help, and GitHub Wiki can drift apart. | `0.3.0` certifies the blocking local help-contract test beside the fixed round-trip, renderer, sanitizer, and browser accessibility corpus. The public Wiki still does not exist because repository publication has not been authorized. | Treat ADR 0019 plus the fixed fixture as the implementation source of truth; review every dialect change across editor, renderer, sanitizers, tests, and in-app help. Before public beta, publish end-user/operator material to the actual public GitHub Wiki, add link/drift validation, and expose stable contextual-help links without injecting remote executable content. | Initial contract `0.2.1`; local stabilization `0.2.9`; documentation certification `0.3.0`; Wiki/contextual help `0.8.8` |
| `TD-RISK-020` | External credential references can be confused with access grants or unsafe share links. | `0.3.1` stores no customer value, rejects unknown/secret-shaped fields, strictly accepts only canonical 1Password Copy Private Links, labels the record and handoff as a pointer, omits links from list/search responses, separates scoped view/manage/open grants, and records empty-metadata audits. A reference can still become stale after a provider item moves, and strict compatibility relies on an inferred provider grammar because no stable machine-readable grammar is published. | Retain the no-retrieval/no-proxy boundary and fixed negative corpus. Provide operator stale-item guidance; fail closed on provider format changes until deliberate adapter review; do not weaken validation to restore compatibility. Re-certify workspace/reference performance, lifecycle, and accessibility with inventory. | Decision ADR 0020; initial implementation `0.3.1`; stabilization `0.3.10` |
| `TD-RISK-021` | Managed attachments are validated but are not malware-scanned or quarantined. | Forced downloads, content signatures, strict allowlists, private storage, and the absence of inline rendering substantially limit exposure, but an authorized user can still upload and another authorized user can download a malicious file whose bytes match an allowed format. Local-volume storage also lacks the provider isolation and lifecycle controls expected of larger deployments. | Keep downloads forced and private; do not add inline active-content rendering or archive extraction. Add a provider-neutral storage boundary, asynchronous malware scanning with quarantine and fail-closed status, scan-result audit metadata without file content, safe retry/failure behavior, and an adversarial file-processing corpus before broadening attachment types. | Initial bounded contract `0.2.6`; implementation and corpus `0.3.8`; stabilization `0.3.10` |
| `TD-RISK-022` | Flexible product specifications can become a validation-denial or forged-provenance boundary. | Arbitrary JSON Schema permits remote resolution, expensive compound evaluation, hostile regular expressions, or ambiguous reinterpretation; matching a client asset by supplier/model name would also make provenance mutable and forgeable. | `0.3.3` restricts schemas to bounded closed objects and a maintained non-executable vocabulary, validates values server-side, pins immutable model revisions to immutable definition versions, requires optimistic concurrency, and enforces exact supplier scope. `0.3.4` retains exact supplier/product/model/revision/schema/publication identifiers, validated values, digests, and a checksum; database guards reject relationship forgery, provenance columns/document rows are immutable, and a narrow read-only RLS projection exposes only supplier catalogs plus associated client-visible STATIC publications to client workflows. No provenance derives from labels. Continue scale, archive, and restore certification through `0.3.10`. | Schema/history mitigated `0.3.3`; provenance edge mitigated `0.3.4`; inventory stabilization `0.3.10` |
| `TD-RISK-023` | Mutable hardware current state can drift from or forge lifecycle history. | A serial, custody target, state, or disposal update performed without a matching event would make operational state and retained evidence disagree; a foreign assignment could also disclose or misattribute a person/location. | `0.3.5` separates one locked current-state profile from an append-only typed ledger, performs mutations transactionally through one service boundary, derives assignment targets from the exact client, makes disposal terminal, keeps sensitive free text out of events/audits, and adds same-scope PostgreSQL guards, forced RLS, immutable triggers, and sibling/cross-tenant negative tests. Re-certify concurrency, scale, archive, upgrade, and restore behavior with the complete inventory subsystem. | Initial mitigation `0.3.5`; inventory stabilization `0.3.10` |
| `TD-RISK-024` | Software entitlement counts or relationships can drift from actual seat allocation. | Concurrent allocation, cross-client targets, mismatched products, or mutable history could over-assign a license or create misleading renewal evidence. | `0.3.6` locks licenses during allocation, counts active seats transactionally, requires explicit same-product installation links, validates exact-client people/installations, records value-minimized append-only events, and adds database edge guards plus forced RLS. Add concurrency stress, expiry automation, archival reconciliation, and reference-data scale evidence at inventory stabilization. | Initial mitigation `0.3.6`; inventory stabilization `0.3.10` |
| `TD-RISK-025` | Financial values can leak through structurally harmless list/search behavior. | Hiding an amount in detail is insufficient if denied users can infer it from field placeholders, cost counts, totals, sort order, result counts, matching labels/references, validation errors, audit metadata, or authors placing pricing in an operational description. | `0.3.7` stores cost lines separately, projects the entire member only through exact-scope `costs.view`, keeps contract search independent of cost rows, requires `assets.edit` plus cost visibility for mutation, emits value-free audit events, warns authors to keep financial terms in protected rows, and adds denial/sibling tests. Expand oracle/property tests, query ceilings, export exclusions, restore evidence, and any future aggregate policy before adding reports or accounting. | Initial mitigation `0.3.7`; inventory stabilization `0.3.10`; reporting/accounting future review |

## Release rule

Release evidence must name every risk assigned to that milestone and record it as `resolved`, `mitigated with remaining owner`, or `blocked`. Moving a risk requires updating both this register and the destination milestone; it may not disappear from a release note merely because feature work passed.
