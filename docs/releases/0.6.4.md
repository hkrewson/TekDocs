# TekDocs 0.6.4 provider connections

Date: 2026-08-12

Status: **local slice complete.** No push, tag, deployment, or remote provider access is implied.

Introduces an exact-Workspace provider registry and read-only NetBox connection. Provider tokens require recent MFA-backed session management, use envelope encryption bound to tenant/connection/generation, and never appear in read responses or audit metadata. Provider URLs reuse the pinned-public-HTTPS, certificate-verified, no-redirect egress boundary.

Evidence is consolidated with the coordinated runtime track in `docs/releases/0.6.8.md`.
