# TekDocs 0.7.0 API and integration certification

Date: 2026-08-12

Status: **local certification complete.** No provider write-back, remote Git operation, push, tag, deployment, or public release is implied.

## Certified boundary

- Public API conventions, deterministic OpenAPI operation IDs, and the generated TypeScript client compose with session and exactly scoped personal/service-token authentication.
- Signed value-minimized webhooks retain replay, bounded-retry, SSRF, and delivery-inspection controls.
- Read-only provider connections, durable page jobs, worker leases/retries, observations, logs, conflicts, and reconciliation remain exact-Workspace scoped under central policy and forced RLS.
- Sanitized Git export produces deterministic local artifacts without credentials, live attachments, provider payloads, remote initialization, commits, authentication, or pushes.
- The certification adds no domain model, migration, route, provider, permission, or UI workflow.

## Evidence

- `make test-integration-certification` composes API contracts, tokens, webhooks, providers, job stabilization, reconciliation, export, permission/IDOR, forced RLS, migration preservation, and frontend tests.
- `make integration-certification-upgrade-rehearsal` preserves the encrypted connection, completed observation, deterministic export, identities, and authorization state from exact `0.6.9` commit `fc8aec7`.
- The independent PostgreSQL/media restore and production-target secret-file/image rehearsals remain green at `0.7.0`.
- `make check` verifies version, supply-chain pins, workflow syntax, Ruff, mypy, migration drift, OpenAPI/client drift, frontend accessibility/unit behavior, and production build budgets.

An administrator controlling both PostgreSQL and the deployment wrapping key can decrypt provider credentials. Providers remain read-only and database-canonical; process-wide structured logging, proxy-aware DAST, supported backup tooling, and third-party connector expansion retain later roadmap owners.
