# TekDocs 0.6.3 signed webhooks

Date: 2026-08-12

Status: **local slice complete.** No push, tag, deployment, or hosted workflow is implied.

## Delivered boundary

- Organization-scoped inbound/outbound endpoints use copy-once, envelope-encrypted, rotatable signing keys and allowlisted topics.
- Canonical requests carry versioned HMAC-SHA256 signatures, delivery IDs, and timestamps. Inbound verification rejects malformed, stale, tampered, duplicate, or unknown endpoints without retaining bodies or signatures.
- Outbound transport requires pinned public HTTPS with certificate hostname/SNI verification, no redirects, and strict time/body bounds.
- Idempotent outbox projection, bounded retries, stale-claim recovery, dead letters, audited manual replay, and metadata-only delivery inspection are available through organization Integrations.
- PostgreSQL guards exact endpoint/event/organization ownership, monotonic history, delivered/receipt immutability, control-plane endpoint identity, and forced delivery RLS.

## Risk disposition

`TD-RISK-047` is **mitigated with remaining owners**. The initial signing, replay, SSRF, scoping, retention, and retry boundaries are implemented. Provider-specific payload semantics/configuration, generalized egress reuse, adversarial proxy/DNS/redirect testing, delivery scale, exact-prior upgrade evidence, anomaly monitoring, and final integration certification remain assigned to `0.6.4`, `0.6.9`, and `0.7.0`.

## Verification

- The focused webhook, workspace, permission/IDOR, forced-RLS, migration, egress, signature, replay, retry, and frontend component/API suites pass. This includes hostile destinations, DNS rebinding, redirects, tampering, expiry, duplicate delivery IDs, cross-organization writes, append-only receipts, and copy-once secret handling.
- `make check` passes Ruff, mypy across 120 source files, migration drift, OpenAPI/generated-client drift, frontend lint/type checks, 205 unit/component tests with 65.14% branch coverage, the production build, bundle budgets, version alignment, and supply-chain pin checks. The Integrations screen is a 9.25 kB lazy chunk so webhook administration does not inflate the initial shell.
- `make test-compose` passes a clean production-shaped image build, readiness, SMTP delivery, deployment checks, Compose provenance, and all 1,499 PostgreSQL backend tests at 86.95% coverage. The only deployment-check warnings are the pre-existing generated OpenAPI operation-name collisions and the intentional local proxy SSL-redirect warning.
- `make security` passes pip-audit, Bandit, npm audit, Gitleaks, and High/Critical Trivy scans for the backend, frontend, and Mailpit images with no findings.
