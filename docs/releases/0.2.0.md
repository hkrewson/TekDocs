# TekDocs 0.2.0 entity/RBAC certification evidence

Date: 2026-08-09

Status: **verified locally.** The complete release gate passed on 2026-08-09. Hosted GitHub execution remains externally blocked pending explicit repository-publication authorization.

## Delivered scope

- Added a maintained certification inventory that classifies every tenant-bearing Django model as forced-RLS domain data, authorization control-plane state, or the installation singleton. Discovery drift and missing fail-closed scoped managers are blocking failures.
- Added PostgreSQL guards that make tenant membership identity and invitation ownership immutable. Invitation actors, organization-assignment creators, access-collection creators, and custom-role creators must belong to the same tenant; the existing scoped-assignment actor guard remains enforced. Runtime startup verifies all five new guards.
- Expanded the generalized permission matrix from one representative unsafe method per route to every unsafe method on every cataloged route for Read-only and MFA denial.
- Added permission/role catalog completeness and privilege-ceiling certification, including explicit exclusions for membership administration and secret reveal.
- Extended migration-cycle evidence to reverse and reapply accounts guard migration `0012` and core RLS migration `0019` without losing representative entity, role, assignment, archive, or audit state.
- Added `make test-certification` and made it a required part of `make release-gate`.
- Added no new domain family and made no product workflow expansion.

## Verification

`make release-gate` passed as one uninterrupted final execution with:

- `make check`: version and dependency-pin consistency, actionlint, Ruff, mypy across 68 source files, migration drift, OpenAPI drift, frontend lint/type checks, and production build passed.
- Host backend suite: 445 passed, 19 expected PostgreSQL-only skips, 85.21% coverage.
- PostgreSQL Compose suite: 464 passed, 91.59% coverage, including the complete entity/RBAC certification, IDOR, runtime RLS, and migration-cycle suites.
- Frontend unit/component suite: 114 passed; coverage remained above the configured thresholds.
- Browser suite: 90 Playwright cases passed across Chromium, Firefox, and WebKit.
- Live integration: the non-mocked Chromium-to-Django-to-PostgreSQL organization-workspace journey passed.
- Security: pip-audit, Bandit, npm audit, Gitleaks, and Trivy HIGH/CRITICAL image checks reported no findings.
- Runtime: the production-target image rehearsal and fresh-install rehearsal passed for `0.2.0`.
- Upgrade: the preserved-database rehearsal from `0.1.3` to `0.2.0` passed, including identity/authentication invariants, Django system checks, and migration-drift verification.

## Risk disposition

- `TD-RISK-001` — **certified for the entity/RBAC foundation.** Future domain and client-portal work must extend the same route, field/audience, assignment, and negative-isolation contracts.
- `TD-RISK-002` — **certified for one MSP per installation.** Every tenant-bearing model now has a blocking isolation classification. Hosted multi-MSP control-plane reads remain outside this claim and require a separate architecture review.
- `TD-RISK-008` — **retained.** Audit mutation remains database-denied and migration-cycle covered.
- `TD-RISK-010` — **externally blocked, not verified.** No push, repository publication, branch-protection change, or hosted workflow execution was authorized.
- `TD-RISK-014` — **unchanged and isolated.** User-owned editor/example changes remain outside this slice and its commit.
- `TD-RISK-017` and `TD-RISK-018` — **recurring gates retained.** Performance and migration preservation evidence remain blocking as the schema grows.

## Known boundaries

- TekDocs `0.2.0` supports one MSP per installation. The schema retains tenant identifiers for future evolution, but this release does not claim hosted multi-MSP identity/control-plane isolation.
- Authentication-provider and authorization control-plane reads remain outside RLS because they establish tenant identity and policy. This is an explicit reviewed exception backed by application isolation and database integrity, not a general exemption for domain data.
- Client portal roles remain cataloged but unavailable for tenant assignment; controlled client access is a later milestone.
- User-owned editor/example work remains intentionally uncommitted and is the `0.2.1` entry criterion.
- No push, tag, deployment, hosted automation, or external publication was performed.
