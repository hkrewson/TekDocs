# TekDocs 0.8.2 supported upgrade matrix

Date: 2026-08-13

Status: **local slice complete.** No production upgrade, rollback, push, tag, deployment, or public release is implied.

## Delivered boundary

- One blocking matrix migrates retained state from the stabilized release of every supported prior minor line: `0.1.3`, `0.2.9`, `0.3.12`, `0.4.9`, `0.5.9`, `0.6.9`, `0.7.13`, and `0.8.0`.
- Source commits are immutable inputs whose checked-in versions are verified before execution.
- Identity/authentication, documentation/publication bytes, inventory, networks, client portal/notifications, integrations, and compliance/monitoring use their established domain fixtures rather than empty-schema smoke checks.
- Existing upgrade rehearsals now target the current release while retaining strict expected-source validation.
- The upgrade and rollback runbook defines pre-upgrade encrypted recovery, forward-only migration behavior, exact failure boundaries, data-loss implications, and prohibited manual repairs.
- The current recovery helper can capture a running older stack without requiring recovery code inside that older backend image.

## Evidence

- `make supported-upgrade-matrix`
- `make supported-recovery-rehearsal`
- `make check`

Rollback remains a recovery operation, not an in-place downgrade. Platform-specific upgrade matrices and final clean-install/every-version release-candidate evidence recur in `0.9.2` and `0.9.3`.
